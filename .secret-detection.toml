# Secret scanner configuration, used by bin/secret-detection.sh.
# Extends the built-in ruleset of Betterleaks rather than replacing it.
[extend]
useDefault = true

[[allowlists]]
paths = [
  '''pnpm-lock\.yaml''',
]
