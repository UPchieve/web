cache:
  key:
    files:
      - package-lock.json
  paths:
    - .npm

variables:
  AUTO_DEVOPS_BUILD_IMAGE_CNB_ENABLED: "true"
  AUTO_DEVOPS_BUILD_IMAGE_CNB_BUILDER: "heroku/buildpacks:20"
  npm_config_cache: "$CI_PROJECT_DIR/.npm"
  NEW_RELIC_ENABLED: "false"
  NEW_RELIC_NO_CONFIG_FILE: "true"

build_production:
  stage: build
  image: "docker:20.10.6"
  cache: {}
  tags:
    - upchieve
    - containerbuild
  variables:
    DOCKER_TLS_CERTDIR: ""
    MOUNT_POINT: /builds/$CI_PROJECT_PATH/mnt
  services:
    - docker:20.10.6-dind
  script:
    - |
      if [[ -z "$CI_COMMIT_TAG" ]]; then
        export CI_APPLICATION_REPOSITORY=${CI_APPLICATION_REPOSITORY:-$CI_REGISTRY_IMAGE/$CI_COMMIT_REF_SLUG}
        export CI_APPLICATION_TAG=${CI_APPLICATION_TAG:-$CI_COMMIT_SHA}
      else
        export CI_APPLICATION_REPOSITORY=${CI_APPLICATION_REPOSITORY:-$CI_REGISTRY_IMAGE}
        export CI_APPLICATION_TAG=${CI_APPLICATION_TAG:-$CI_COMMIT_TAG}
      fi
    - export image_tagged="$CI_APPLICATION_REPOSITORY:$CI_APPLICATION_TAG"
    - export image_cache="$CI_REGISTRY_IMAGE/cache-image:latest"
    - export image_latest="$CI_APPLICATION_REPOSITORY:latest"
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - apk add --update curl tar
    # install buildpacks tool
    - (curl -sSL "https://github.com/buildpacks/pack/releases/download/v0.26.0/pack-v0.26.0-linux.tgz" | tar -C /usr/local/bin/ --no-same-owner -xzv pack)
    # install and configure doppler cli
    - wget -q -t3 'https://packages.doppler.com/public/cli/rsa.8004D9FF50437357.key' -O /etc/apk/keys/cli@doppler-8004D9FF50437357.rsa.pub
    - echo 'https://packages.doppler.com/public/cli/alpine/any-version/main' | tee -a /etc/apk/repositories
    - apk add doppler
    - mkdir -p "$MOUNT_POINT"
    # build
    - |
      doppler run --token "$DOPPLER_TOKEN_PRODUCTION" \
      --mount "$MOUNT_POINT/.doppler_env" \
      --mount-template .doppler_env.tmpl -- \
          pack build "$image_tagged"-production \
            --env-file "$MOUNT_POINT/.doppler_env" \
            --env "VUE_APP_VERSION=$CI_COMMIT_SHA-production" \
            --creation-time now \
            --builder heroku/buildpacks:20 \
            --buildpack registry.gitlab.com/upchieve/doppler-buildpack \
            --buildpack heroku/nodejs@1.1.7 \
            --buildpack heroku/procfile@0.6.2
    - docker push "$image_tagged"-production
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'

build_demo:
  stage: build
  image: "docker:20.10.6"
  cache: {}
  tags:
    - upchieve
    - containerbuild
  when: manual
  allow_failure: true
  variables:
    DOCKER_TLS_CERTDIR: ""
    MOUNT_POINT: /builds/$CI_PROJECT_PATH/mnt
  services:
    - docker:20.10.6-dind
  script:
    - |
      if [[ -z "$CI_COMMIT_TAG" ]]; then
        export CI_APPLICATION_REPOSITORY=${CI_APPLICATION_REPOSITORY:-$CI_REGISTRY_IMAGE/$CI_COMMIT_REF_SLUG}
        export CI_APPLICATION_TAG=${CI_APPLICATION_TAG:-$CI_COMMIT_SHA}
      else
        export CI_APPLICATION_REPOSITORY=${CI_APPLICATION_REPOSITORY:-$CI_REGISTRY_IMAGE}
        export CI_APPLICATION_TAG=${CI_APPLICATION_TAG:-$CI_COMMIT_TAG}
      fi
    - export image_tagged="$CI_APPLICATION_REPOSITORY:$CI_APPLICATION_TAG"
    - export image_cache="$CI_REGISTRY_IMAGE/cache-image:latest"
    - export image_latest="$CI_APPLICATION_REPOSITORY:latest"
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - apk add --update curl tar
    # install buildpacks tool
    - (curl -sSL "https://github.com/buildpacks/pack/releases/download/v0.26.0/pack-v0.26.0-linux.tgz" | tar -C /usr/local/bin/ --no-same-owner -xzv pack)
    # install and configure doppler cli
    - wget -q -t3 'https://packages.doppler.com/public/cli/rsa.8004D9FF50437357.key' -O /etc/apk/keys/cli@doppler-8004D9FF50437357.rsa.pub
    - echo 'https://packages.doppler.com/public/cli/alpine/any-version/main' | tee -a /etc/apk/repositories
    - apk add doppler
    - mkdir -p "$MOUNT_POINT"
    # build
    - |
      doppler run --token "$DOPPLER_TOKEN_DEMO" \
      --mount "$MOUNT_POINT/.doppler_env" \
      --mount-template .doppler_env.tmpl -- \
          pack build "$image_tagged"-demo \
            --env-file "$MOUNT_POINT/.doppler_env" \
            --env "VUE_APP_VERSION=$CI_COMMIT_SHA-demo" \
            --creation-time now \
            --builder heroku/buildpacks:20 \
            --buildpack registry.gitlab.com/upchieve/doppler-buildpack \
            --buildpack heroku/nodejs@1.1.7 \
            --buildpack heroku/procfile@0.6.2
    - docker push "$image_tagged"-demo
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'

build_staging:
  stage: build
  image: "docker:20.10.6"
  cache: {}
  tags:
    - upchieve
    - containerbuild
  variables:
    DOCKER_TLS_CERTDIR: ""
    MOUNT_POINT: /builds/$CI_PROJECT_PATH/mnt
  services:
    - docker:20.10.6-dind
  script:
    - |
      if [[ -z "$CI_COMMIT_TAG" ]]; then
        export CI_APPLICATION_REPOSITORY=${CI_APPLICATION_REPOSITORY:-$CI_REGISTRY_IMAGE/$CI_COMMIT_REF_SLUG}
        export CI_APPLICATION_TAG=${CI_APPLICATION_TAG:-$CI_COMMIT_SHA}
      else
        export CI_APPLICATION_REPOSITORY=${CI_APPLICATION_REPOSITORY:-$CI_REGISTRY_IMAGE}
        export CI_APPLICATION_TAG=${CI_APPLICATION_TAG:-$CI_COMMIT_TAG}
      fi
    - export image_tagged="$CI_APPLICATION_REPOSITORY:$CI_APPLICATION_TAG"
    - export image_cache="$CI_REGISTRY_IMAGE/cache-image:latest"
    - export image_latest="$CI_APPLICATION_REPOSITORY:latest"
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - apk add --update curl tar
    # install buildpacks tool
    - (curl -sSL "https://github.com/buildpacks/pack/releases/download/v0.26.0/pack-v0.26.0-linux.tgz" | tar -C /usr/local/bin/ --no-same-owner -xzv pack)
    # install and configure doppler cli
    - wget -q -t3 'https://packages.doppler.com/public/cli/rsa.8004D9FF50437357.key' -O /etc/apk/keys/cli@doppler-8004D9FF50437357.rsa.pub
    - echo 'https://packages.doppler.com/public/cli/alpine/any-version/main' | tee -a /etc/apk/repositories
    - apk add doppler
    - mkdir -p "$MOUNT_POINT"
      # build
    - |
      doppler run --token "$DOPPLER_TOKEN_STAGING" \
      --mount "$MOUNT_POINT/.doppler_env" \
      --mount-template .doppler_env.tmpl -- \
          pack build "$image_tagged"-staging \
            --env-file "$MOUNT_POINT/.doppler_env" \
            --env "VUE_APP_VERSION=$CI_COMMIT_SHA-staging" \
            --creation-time now \
            --builder heroku/buildpacks:20 \
            --buildpack registry.gitlab.com/upchieve/doppler-buildpack \
            --buildpack heroku/nodejs@1.1.7 \
            --buildpack heroku/procfile@0.6.2
    - docker push "$image_tagged"-staging
  rules:
    - if: '$CI_COMMIT_BRANCH != "main"'
      when: always
    - if: '$CI_COMMIT_BRANCH == "main"'
      when: manual

build_hackers:
  stage: build
  image: "docker:20.10.6"
  cache: {}
  tags:
    - upchieve
    - containerbuild
  when: manual
  allow_failure: true
  variables:
    DOCKER_TLS_CERTDIR: ""
    MOUNT_POINT: /builds/$CI_PROJECT_PATH/mnt
  services:
    - docker:20.10.6-dind
  script:
    - |
      if [[ -z "$CI_COMMIT_TAG" ]]; then
        export CI_APPLICATION_REPOSITORY=${CI_APPLICATION_REPOSITORY:-$CI_REGISTRY_IMAGE/$CI_COMMIT_REF_SLUG}
        export CI_APPLICATION_TAG=${CI_APPLICATION_TAG:-$CI_COMMIT_SHA}
      else
        export CI_APPLICATION_REPOSITORY=${CI_APPLICATION_REPOSITORY:-$CI_REGISTRY_IMAGE}
        export CI_APPLICATION_TAG=${CI_APPLICATION_TAG:-$CI_COMMIT_TAG}
      fi
    - export image_tagged="$CI_APPLICATION_REPOSITORY:$CI_APPLICATION_TAG"
    - export image_cache="$CI_REGISTRY_IMAGE/cache-image:latest"
    - export image_latest="$CI_APPLICATION_REPOSITORY:latest"
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - apk add --update curl tar
    # install buildpacks tool
    - (curl -sSL "https://github.com/buildpacks/pack/releases/download/v0.26.0/pack-v0.26.0-linux.tgz" | tar -C /usr/local/bin/ --no-same-owner -xzv pack)
    # install and configure doppler cli
    - wget -q -t3 'https://packages.doppler.com/public/cli/rsa.8004D9FF50437357.key' -O /etc/apk/keys/cli@doppler-8004D9FF50437357.rsa.pub
    - echo 'https://packages.doppler.com/public/cli/alpine/any-version/main' | tee -a /etc/apk/repositories
    - apk add doppler
    - mkdir -p "$MOUNT_POINT"
    # build
    - |
      doppler run --token "$DOPPLER_TOKEN_HACKERS" \
      --mount "$MOUNT_POINT/.doppler_env" \
      --mount-template .doppler_env.tmpl -- \
          pack build "$image_tagged"-hackers \
            --env-file "$MOUNT_POINT/.doppler_env" \
            --env "VUE_APP_VERSION=$CI_COMMIT_SHA-hackers" \
            --creation-time now \
            --builder heroku/buildpacks:20 \
            --buildpack registry.gitlab.com/upchieve/doppler-buildpack \
            --buildpack heroku/nodejs@1.1.7 \
            --buildpack heroku/procfile@0.6.2
    - docker push "$image_tagged"-hackers
  rules:
    - if: '$CI_COMMIT_BRANCH != "main"'

test:
  stage: test
  image: node:16.16.0-bullseye
  tags:
    - upchieve
  before_script:
    - npm ci --cache .npm --prefer-offline
  script:
    - npm run test
  needs: []
  rules:
    - if: '$TEST_DISABLED'
      when: never
    - if: '$CI_COMMIT_TAG || $CI_COMMIT_BRANCH'

lint:
  stage: test
  image: node:16.16.0-bullseye
  tags:
    - upchieve
  before_script:
    - npm ci --cache .npm --prefer-offline
  script:
    - npm run lint:dry
  needs: []
  rules:
    - if: '$TEST_DISABLED'
      when: never
    - if: '$CI_COMMIT_TAG || $CI_COMMIT_BRANCH'

html_validate:
  stage: test
  image: node:16.16.0-bullseye
  allow_failure: true
  tags:
    - upchieve
  before_script:
    - npm ci --cache .npm --prefer-offline
  script:
    - npm run html-validate
  needs: []
  rules:
    - if: '$TEST_DISABLED'
      when: never
    - if: '$CI_COMMIT_TAG || $CI_COMMIT_BRANCH'
